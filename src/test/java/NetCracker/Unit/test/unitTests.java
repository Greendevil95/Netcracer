/*package NetCracker.Unit.test;

import java.util.GregorianCalendar;

public class unitTests {
    @Test
    public void test() {
        LocalDate inputBirthDate = LocalDate.fromGregorianCalendarFields(new GregorianCalendar(2010, 11, 1));
        Person p = new Person("XXX", inputBirthDate);
        Integer expetedAge = 8;
        Integer age = p.getAge();
        assertEquals(expectedAge, Age);

    }
}
    @Before
    public void localSetup()
    {
       // вып каждый раз перед запуском
    }

@BeforClass //Один раз

@After //после вып каждого теста

@AfterClass //после всех тестов

//Юнит тесты пишутся для каждого класса*/





